<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Gestion des clubs - Admin ClubEdge">
    <title>Gestion des clubs - Admin - ClubEdge</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Styles existants -->
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/variables.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/reset.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/layout.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/components.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/utilities.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/animations.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/responsive.css">
    <!-- Utilisation du style des clubs pour la cohérence -->
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/pages/clubs.css">
    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/pages/admin.css">
</head>

<body>
    <div class="app-layout">
        <!-- Sidebar (Style de la page 2 avec badge Admin) -->
        <aside class="app-sidebar" id="sidebar">
            <div class="sidebar-header">
                <a href="admin/dashboard" class="sidebar-logo">
                    <svg width="32" height="32" viewBox="0 0 40 40" fill="none">
                        <rect width="40" height="40" rx="4" fill="#FF4F00" />
                        <path
                            d="M12 28V12H20C22.12 12 24.16 12.84 25.66 14.34C27.16 15.84 28 17.88 28 20C28 22.12 27.16 24.16 25.66 25.66C24.16 27.16 22.12 28 20 28H12Z"
                            stroke="white" stroke-width="2" />
                    </svg>
                    <span class="sidebar-logo-text">ClubEdge</span>
                </a>
                <span class="badge badge-accent ml-2">Admin</span>
            </div>

            <nav class="sidebar-nav">
                <ul class="sidebar-menu">
                    <li class="sidebar-item">
                        <a href="dashboard" class="sidebar-link active">
                            <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="1.5">
                                <rect x="3" y="3" width="7" height="9" rx="1" />
                                <rect x="14" y="3" width="7" height="5" rx="1" />
                                <rect x="14" y="12" width="7" height="9" rx="1" />
                                <rect x="3" y="16" width="7" height="5" rx="1" />
                            </svg>
                            <span>Tableau de bord</span>
                        </a>
                    </li>
                </ul>

                <div class="sidebar-section">
                    <h3 class="sidebar-section-title">Gestion</h3>
                    <ul class="sidebar-menu">
                        <li class="sidebar-item">
                            <a href="clubs" class="sidebar-link">
                                <span>Clubs</span>
                            </a>
                        </li>
                        <li class="sidebar-item">
                            <a href="users" class="sidebar-link">
                                <span>Utilisateurs</span>
                            </a>
                        </li>
                        <li class="sidebar-item">
                            <a href="events" class="sidebar-link">
                                <span>Événements</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        </aside>

        <div class="app-main">
            <header class="app-header">
                <button class="btn btn-icon btn-ghost" id="menu-toggle">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                        <line x1="3" y1="12" x2="21" y2="12" /><line x1="3" y1="6" x2="21" y2="6" /><line x1="3" y1="18" x2="21" y2="18" />
                    </svg>
                </button>
                <div class="flex-1"></div>
                <a href="logout" class="btn btn-outline btn-sm">Déconnexion</a>
            </header>

            <main class="app-content">
                <!-- Page Header (Inspiré page 2) -->
                <div class="page-header flex items-center justify-between">
                    <div>
                        <h1 class="page-title">Gestion des Clubs</h1>
                        <p class="page-description">Supervisez, modifiez et gérez les clubs de l'établissement.</p>
                    </div>
                   
                </div>

                <!-- Toolbar (Même design que page 2) -->
                <div class="clubs-toolbar mb-8">
                    <div class="clubs-search">
                        <svg class="clubs-search-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                            <circle cx="11" cy="11" r="8" /><line x1="21" y1="21" x2="16.65" y2="16.65" />
                        </svg>
                        <input type="text" class="form-input clubs-search-input" placeholder="Rechercher par nom ou président...">
                    </div>
                     <a href="/ClubEdge/admin/clubs/create" class="btn btn-primary">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="12" y1="5" x2="12" y2="19" /><line x1="5" y1="12" x2="19" y2="12" />
                        </svg>
                        Nouveau Club
                    </a>
                </div>

                <!-- Clubs Grid (Le concept de la page 2 appliqué à l'admin) -->
                <div class="grid grid-cols-3 gap-6 stagger-animation">
                    {% for club in clubs %}
                    <div class="club-card">
                        <div class="club-card-header">
                            <div class="club-card-cover" style="background: linear-gradient(135deg, #FF4F00 0%, #ff8547 100%);"></div>
                            {# <span class="club-card-badge badge badge-success">Actif</span> #}
                        </div>
                        <div class="club-card-body">
                            <div class="avatar avatar-lg club-card-avatar">CI</div>
                            <h3 class="club-card-title">{{club.name}}</h3>
                            <p class="text-xs text-muted mb-2">Président : Marie Martin</p>
                            <p class="club-card-description">{{club.description}}</p>
                            
                            <div class="club-card-stats mb-4">
                                <div class="club-card-stat justify-between
                                $^poiuytr   ">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" /><circle cx="12" cy="7" r="4" />
                                    </svg>
                                    <span>6/8 membres</span>
                                    <a href="/ClubEdge/admin/club-detail?idC={{club.id}}" class="btn-view">Voir détails →</a>
                                </div>

                            </div>
                            <div class="flex gap-2 pt-4 border-t border-dashed">
                                <a href="/ClubEdge/admin/editClub?id={{ club.id }}" class="btn btn-outline btn-sm flex-1">
                                    Modifier
                                </a>
                                <a href="/ClubEdge/admin/deleteClub?id={{ club.id }}" class="btn btn-icon btn-ghost btn-sm text-error" title="Supprimer" onclick="return confirm('Voulez-vous vraiment supprimer ce club ?');">
                                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                                        <polyline points="3 6 5 6 21 6" />
                                        <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" />
                                    </svg>
                                </a>
                            </div>

                        </div>

                    </div>
                    {% endfor %}
                    
                </div>

                <!-- Pagination (Inspirée du style minimaliste) -->
                <div class="pagination-wrapper mt-10">
                    <span class="text-sm text-muted">Affichage de <strong>1-2</strong> sur <strong>6</strong> clubs</span>
                    <div class="pagination">
                        <button class="pagination-item" disabled><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6" /></svg></button>
                        <button class="pagination-item active">1</button>
                        <button class="pagination-item">2</button>
                        <button class="pagination-item"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6" /></svg></button>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Modals & Scripts restent identiques -->
    <div class="modal-backdrop" data-modal-backdrop="create-club-modal"></div>
    <div class="modal" id="create-club-modal">
        <div class="modal-header">
            <h2 class="modal-title">Créer un club</h2>
            <button class="btn btn-icon btn-ghost" data-modal-close>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" />
                </svg>
            </button>
        </div>
        <div class="modal-body">
            <form>
                <div class="form-group mb-4">
                    <label class="form-label">Nom du club</label>
                    <input type="text" class="form-input" placeholder="Ex: Club Robotique">
                </div>
                <div class="form-group mb-4">
                    <label class="form-label">Président (Email ou ID)</label>
                    <input type="text" class="form-input" placeholder="chercher un étudiant...">
                </div>
                <div class="form-group">
                    <label class="form-label">Description</label>
                    <textarea class="form-input form-textarea" rows="3" placeholder="Décrivez le club..."></textarea>
                </div>
            </form>
        </div>
        <div class="modal-footer">
            <button class="btn btn-outline" data-modal-close>Annuler</button>
            <button class="btn btn-primary">Créer le club</button>
        </div>
    </div>

    <div class="mobile-overlay" id="mobile-overlay"></div>
    <script src="/../ClubEdge/public/assets/js/main.js" defer></script>
</body>
</html>