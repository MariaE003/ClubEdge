<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Modifier étudiant - Admin ClubEdge">
    <title>Modifier Étudiant - Admin - ClubEdge</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="/../ClubEdge/public/assets/css/admin.css">
</head>

<body>
<div class="app-layout">

    {# SIDEBAR – inchangée #}
    {% include 'admin/_sidebar.twig' %}

    <div class="app-main">
        <header class="app-header">
            <button class="btn btn-icon btn-ghost" id="menu-toggle">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <line x1="3" y1="12" x2="21" y2="12" />
                    <line x1="3" y1="6" x2="21" y2="6" />
                    <line x1="3" y1="18" x2="21" y2="18" />
                </svg>
            </button>
            <h1 class="text-lg font-semibold ml-4">Modifier l'étudiant</h1>
            <div class="flex-1"></div>
            <button class="btn btn-outline btn-sm">Déconnexion</button>
        </header>

        <main class="app-content">
            <div class="container-sm mx-auto">

                <div class="mb-8">
                    <a href="/admin/students"
                       class="flex items-center gap-2 text-sm text-secondary hover:text-primary transition-colors mb-4">
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                            <path d="M19 12H5" />
                            <path d="M12 19l-7-7 7-7" />
                        </svg>
                        Retour à la gestion des étudiants
                    </a>

                    <h1 class="page-title">
                        Modifier {{ student.nom }} {{ student.prenom }}
                    </h1>

                    <p class="page-description">
                        Compte étudiant créé le {{ student.date_creation|date('Y/m/d') }}
                    </p>
                </div>

                <div class="card">
                    <div class="card-body">
                        <form class="space-y-6" method="post" action="/admin/updateInfoUser">
                            <input type="hidden" name="id" value="{{ student.id }}">

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

                                <div class="form-group">
                                    <label class="form-label">Nom</label>
                                    <input type="text" name="nom" class="form-input" value="{{ student.nom }}">
                                </div>

                                <div class="form-group">
                                    <label class="form-label">Prenom</label>
                                    <input type="text" name="prenom" class="form-input" value="{{ student.prenom }}">
                                </div>

                                <div class="form-group">
                                    <label class="form-label">Email</label>
                                    <input type="email" name="email" class="form-input" value="{{ student.email }}">
                                </div>

                                <div class="form-group">
                                    <label class="form-label">image url</label>
                                    <input type="url" name="profile" class="form-input" value="{{ student.profile }}">
                                </div>

                                <div class="form-group">
                                    <label class="form-label">Club Assigné</label>
                                    <select class="form-input form-select">
                                        <option value="">Aucun</option>
                                        <option selected>
                                            {{ student.club.nom }}
                                        </option>
                                    </select>
                                </div>

                                <div class="form-group">
                                    <label class="form-label">Réinitialisation du mot de passe</label>
                                    <button type="button"
                                            class="btn btn-outline w-full text-left justify-between">
                                        <span>Envoyer un email de réinitialisation</span>
                                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none"
                                             stroke="currentColor" stroke-width="1.5">
                                            <path d="M22 16.92v3a2 2 0 0 1-2.18 2" />
                                        </svg>
                                    </button>
                                </div>

                            </div>

                            <div class="flex justify-end gap-3 pt-4 border-t">
                                <a href="/admin/students" class="btn btn-ghost">Annuler</a>
                                <button type="submit" class="btn btn-primary">
                                    Enregistrer les modifications
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

            </div>
        </main>
    </div>
</div>

<script src="/../ClubEdge/public/assets/js/main.js" defer></script>
</body>
</html>
