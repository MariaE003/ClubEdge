<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Détails de l'événement - ClubEdge">
    <title>Détails de l'événement - ClubEdge</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="{{ asset('css/variables.css') }}">
    <link rel="stylesheet" href="{{ asset('css/reset.css') }}">
    <link rel="stylesheet" href="{{ asset('css/layout.css') }}">
    <link rel="stylesheet" href="{{ asset('css/components.css') }}">
    <link rel="stylesheet" href="{{ asset('css/utilities.css') }}">
    <link rel="stylesheet" href="{{ asset('css/animations.css') }}">
    <link rel="stylesheet" href="{{ asset('css/responsive.css') }}">
    <link rel="stylesheet" href="{{ asset('css/pages/events.css') }}">
</head>

<body>
    <div class="app-layout">
        <aside class="app-sidebar" id="sidebar">
            <div class="sidebar-header">
                <a href="{{ path('home') }}" class="sidebar-logo">
                    <svg width="32" height="32" viewBox="0 0 40 40" fill="none">
                        <rect width="40" height="40" rx="4" fill="#FF4F00" />
                        <path
                            d="M12 28V12H20C22.12 12 24.16 12.84 25.66 14.34C27.16 15.84 28 17.88 28 20C28 22.12 27.16 24.16 25.66 25.66C24.16 27.16 22.12 28 20 28H12Z"
                            stroke="white" stroke-width="2" />
                    </svg>
                    <span class="sidebar-logo-text">ClubEdge</span>
                </a>
            </div>

            <nav class="sidebar-nav">
                <ul class="sidebar-menu">
                    <li class="sidebar-item">
                        <a href="{{ path('etudiant/dashboard') }}" class="sidebar-link">
                            <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="1.5">
                                <rect x="3" y="3" width="7" height="9" rx="1" />
                                <rect x="14" y="3" width="7" height="5" rx="1" />
                                <rect x="14" y="12" width="7" height="9" rx="1" />
                                <rect x="3" y="16" width="7" height="5" rx="1" />
                            </svg>
                            <span>Tableau de bord</span>
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="{{ path('etudiant/events') }}" class="sidebar-link active">
                            <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="1.5">
                                <rect x="3" y="4" width="18" height="18" rx="2" />
                                <line x1="16" y1="2" x2="16" y2="6" />
                                <line x1="8" y1="2" x2="8" y2="6" />
                                <line x1="3" y1="10" x2="21" y2="10" />
                            </svg>
                            <span>Événements</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <div class="app-main">
            <header class="app-header">
                <div class="header-left">
                    <h1 class="header-title">{{ event.title ?? '' }}</h1>
                </div>
                <div class="header-right">
                    <a href="{{ path('etudiant/events') }}" class="btn btn-ghost">Retour</a>
                </div>
            </header>

            <main class="app-content">
                {% if flash is defined and flash and flash.message %}
                <div class="alert {{ flash.type == 'success' ? 'alert-success' : 'alert-error' }} mb-6">
                    {{ flash.message }}
                </div>
                {% endif %}

                <div class="grid grid-cols-12 gap-6">
                    <div class="col-span-8">
                        <div class="card mb-6">
                            <div class="card-body">
                                <div class="grid gap-2">
                                    <p><strong>Club:</strong> {{ event.club_name ?? '' }}</p>
                                    <p><strong>Date:</strong> {{ event.event_date ?? '' }}</p>
                                    <p><strong>Lieu:</strong> {{ event.location ?? '' }}</p>
                                    <p><strong>Participants:</strong> {{ event.participants_count ?? 0 }}</p>
                                    {% if event.description ?? false %}
                                    <p><strong>Description:</strong> {{ event.description }}</p>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        {% if event.images is defined and event.images is not empty %}
                        <div class="card">
                            <div class="card-header">
                                <h2 class="card-title">Galerie</h2>
                            </div>
                            <div class="card-body">
                                <div class="grid grid-cols-3 gap-3">
                                    {% for img in event.images %}
                                    <img src="{{ img }}" alt=""
                                        style="width:100%;height:120px;object-fit:cover;border-radius:8px;">
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                        {% endif %}
                    </div>

                    <div class="col-span-4">
                        <div class="card mb-6">
                            <div class="card-header">
                                <h2 class="card-title">Participants</h2>
                            </div>
                            <div class="card-body">
                                {% if participants is empty %}
                                <p class="text-sm text-muted">Aucun participant.</p>
                                {% else %}
                                <div class="flex flex-col gap-3">
                                    {% for p in participants|slice(0, 5) %}
                                    <div class="flex items-center gap-3">
                                        <div class="avatar avatar-sm">
                                            {{ (p.prenom|default('')|slice(0, 1) ~ p.nom|default('')|slice(0, 1))|upper }}
                                        </div>
                                        <span class="text-sm font-medium">{{ p.prenom ?? '' }} {{ p.nom ?? '' }}</span>
                                    </div>
                                    {% endfor %}
                                    {% if participants|length > 5 %}
                                    <div class="flex items-center gap-3">
                                        <div class="avatar avatar-sm">+{{ participants|length - 5 }}</div>
                                        <span class="text-sm text-muted">Autres participants</span>
                                    </div>
                                    {% endif %}
                                </div>
                                {% endif %}
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header">
                                <h2 class="card-title">Actions</h2>
                            </div>
                            <div class="card-body space-y-3">
                                {% if event.is_past ?? false %}
                                <button class="btn btn-secondary w-full" disabled>Inscriptions fermées</button>
                                {% elseif event.is_joined ?? false %}
                                <button class="btn btn-outline w-full" disabled>Déjà inscrit</button>
                                {% else %}
                                <form method="post" action="{{ path('etudiant/events/' ~ event.id ~ '/join') }}">
                                    <input type="hidden" name="_csrf" value="{{ csrf_token() }}">
                                    <button class="btn btn-primary w-full" type="submit">S'inscrire</button>
                                </form>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </main>

            <footer class="app-footer">
                <p class="text-sm text-muted">2026 ClubEdge. Tous droits réservés.</p>
            </footer>
        </div>
    </div>

    <div class="mobile-overlay" id="mobile-overlay"></div>
    <script src="{{ asset('js/main.js') }}" defer></script>
</body>

</html>

