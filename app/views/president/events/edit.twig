<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Modifier un événement - Espace Président">
    <title>Modifier un événement - Espace Président</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="{{ asset('css/variables.css') }}">
    <link rel="stylesheet" href="{{ asset('css/reset.css') }}">
    <link rel="stylesheet" href="{{ asset('css/layout.css') }}">
    <link rel="stylesheet" href="{{ asset('css/components.css') }}">
    <link rel="stylesheet" href="{{ asset('css/utilities.css') }}">
    <link rel="stylesheet" href="{{ asset('css/animations.css') }}">
    <link rel="stylesheet" href="{{ asset('css/responsive.css') }}">
    <link rel="stylesheet" href="{{ asset('css/pages/dashboard.css') }}">
</head>

<body>
    {% set existing_cover = (event.images is defined and event.images is not empty) ? event.images[0] : null %}

    <div class="app-layout">
        <aside class="app-sidebar" id="sidebar">
            <div class="sidebar-header">
                <a href="{{ path('home') }}" class="sidebar-logo">
                    <svg width="32" height="32" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect width="40" height="40" rx="4" fill="#FF4F00" />
                        <path
                            d="M12 28V12H20C22.1217 12 24.1566 12.8429 25.6569 14.3431C27.1571 15.8434 28 17.8783 28 20C28 22.1217 27.1571 24.1566 25.6569 25.6569C24.1566 27.1571 22.1217 28 20 28H12Z"
                            stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                    </svg>
                    <span class="sidebar-logo-text">ClubEdge</span>
                </a>
            </div>

            <nav class="sidebar-nav">
                <ul class="sidebar-menu">
                    <li class="sidebar-item">
                        <a href="{{ path('president/dashboard') }}" class="sidebar-link">
                            <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="1.5">
                                <rect x="3" y="3" width="7" height="9" rx="1" />
                                <rect x="14" y="3" width="7" height="5" rx="1" />
                                <rect x="14" y="12" width="7" height="9" rx="1" />
                                <rect x="3" y="16" width="7" height="5" rx="1" />
                            </svg>
                            <span>Tableau de bord</span>
                        </a>
                    </li>
                </ul>

                <div class="sidebar-section">
                    <h3 class="sidebar-section-title">Événements</h3>
                    <ul class="sidebar-menu">
                        <li class="sidebar-item">
                            <a href="{{ path('president/events') }}" class="sidebar-link active">
                                <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                    stroke-width="1.5">
                                    <rect x="3" y="4" width="18" height="18" rx="2" />
                                    <line x1="16" y1="2" x2="16" y2="6" />
                                    <line x1="8" y1="2" x2="8" y2="6" />
                                    <line x1="3" y1="10" x2="21" y2="10" />
                                </svg>
                                <span>Mes événements</span>
                            </a>
                        </li>
                        <li class="sidebar-item">
                            <a href="{{ path('president/events/create') }}" class="sidebar-link">
                                <svg class="sidebar-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                    stroke-width="1.5">
                                    <circle cx="12" cy="12" r="10" />
                                    <line x1="12" y1="8" x2="12" y2="16" />
                                    <line x1="8" y1="12" x2="16" y2="12" />
                                </svg>
                                <span>Créer un événement</span>
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>

            <div class="sidebar-footer">
                <div class="sidebar-user">
                    <div class="avatar avatar-sm">PR</div>
                    <div class="sidebar-user-info">
                        <span class="sidebar-user-name">Président</span>
                        <span class="sidebar-user-role">Club</span>
                    </div>
                </div>
            </div>
        </aside>

        <div class="app-main">
            <header class="app-header">
                <button class="btn btn-icon btn-ghost lg:hidden" id="menu-toggle" aria-label="Ouvrir le menu">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                        stroke-width="1.5">
                        <line x1="3" y1="12" x2="21" y2="12" />
                        <line x1="3" y1="6" x2="21" y2="6" />
                        <line x1="3" y1="18" x2="21" y2="18" />
                    </svg>
                </button>
                <span class="badge badge-accent ml-4">Espace Président</span>
                <div class="flex-1"></div>
                <div class="avatar avatar-sm">PR</div>
            </header>

            <main class="app-content">
                <nav class="breadcrumb mb-6" aria-label="Fil d'Ariane">
                    <ol class="breadcrumb-list flex items-center gap-2" role="list">
                        <li class="breadcrumb-item flex items-center">
                            <a href="{{ path('president/events') }}"
                                class="breadcrumb-link text-sm text-secondary hover:text-primary">Mes événements</a>
                        </li>
                        <li class="breadcrumb-separator flex items-center text-muted" aria-hidden="true">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                                stroke-width="1.5">
                                <polyline points="9 18 15 12 9 6" />
                            </svg>
                        </li>
                        <li class="breadcrumb-item flex items-center">
                            <span class="breadcrumb-current text-sm font-medium">Modifier</span>
                        </li>
                    </ol>
                </nav>

                <div class="page-header mb-8">
                    <h1 class="page-title">Modifier l'événement</h1>
                    <p class="page-description">{{ event.title ?? '' }}</p>
                </div>

                <div class="card mb-6">
                    <div class="card-header">
                        <h2 class="card-title">Images actuelles</h2>
                    </div>
                    <div class="card-body">
                        {% if event.images is empty %}
                        <p class="text-sm text-muted">Aucune image.</p>
                        {% else %}
                        <div class="grid grid-cols-4 gap-3">
                            {% for img in event.images %}
                            <img src="{{ img }}" alt="" style="width:100%;height:90px;object-fit:cover;border-radius:8px;">
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                </div>

                <div class="card">
                    <div class="card-body">
                        <form method="post" action="{{ path('president/events/' ~ event.id ~ '/update') }}"
                            enctype="multipart/form-data" class="event-form" novalidate>
                            <input type="hidden" name="_csrf" value="{{ csrf_token() }}">

                            <div class="form-section mb-8">
                                <h3 class="form-section-title">Informations</h3>

                                <div class="form-group">
                                    <label for="event-title" class="form-label">Titre *</label>
                                    <input type="text" id="event-title" name="title" class="form-input"
                                        value="{{ old.title ?? event.title ?? '' }}" required>
                                    {% if errors.title ?? false %}
                                    <p class="form-error">{{ errors.title }}</p>
                                    {% endif %}
                                </div>

                                <div class="form-group">
                                    <label for="event-description" class="form-label">Description</label>
                                    <textarea id="event-description" name="description" class="form-input form-textarea"
                                        rows="5" placeholder="Décrivez l'événement...">{{ old.description ?? event.description ?? '' }}</textarea>
                                </div>
                            </div>

                            <div class="form-section mb-8">
                                <h3 class="form-section-title">Date</h3>

                                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                                    <div class="form-group">
                                        <label for="event-date" class="form-label">Date et heure *</label>
                                        <input type="datetime-local" id="event-date" name="event_date" class="form-input"
                                            value="{{ old.event_date ?? '' }}" required>
                                        {% if errors.event_date ?? false %}
                                        <p class="form-error">{{ errors.event_date }}</p>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>

                            <div class="form-section mb-8">
                                <h3 class="form-section-title">Lieu</h3>

                                <div class="form-group">
                                    <label for="event-location" class="form-label">Salle / Lieu</label>
                                    <input type="text" id="event-location" name="location" class="form-input"
                                        value="{{ old.location ?? event.location ?? '' }}">
                                    {% if errors.location ?? false %}
                                    <p class="form-error">{{ errors.location }}</p>
                                    {% endif %}
                                </div>
                            </div>

                            <div class="form-section mb-8">
                                <h3 class="form-section-title">Image de couverture</h3>

                                <div class="form-group">
                                    <div class="upload-zone aspect-video {{ existing_cover ? 'has-preview' : '' }}">
                                        <img id="event-image-preview"
                                            class="upload-preview {{ existing_cover ? '' : 'hidden' }}"
                                            alt="Aperçu de l'image" {% if existing_cover %}src="{{ existing_cover }}"{% endif %}>
                                        <div id="event-image-placeholder"
                                            class="upload-placeholder {{ existing_cover ? 'hidden' : '' }}">
                                            <svg class="upload-icon" viewBox="0 0 24 24" fill="none"
                                                stroke="currentColor" stroke-width="1.5">
                                                <rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
                                                <circle cx="8.5" cy="8.5" r="1.5" />
                                                <polyline points="21 15 16 10 5 21" />
                                            </svg>
                                            <p class="upload-text">Glissez-déposez une image ou <span
                                                    class="text-accent">parcourez</span></p>
                                            <p class="upload-hint">PNG, JPG jusqu'à 5MB</p>
                                        </div>
                                        <div class="upload-overlay" aria-hidden="true">Changer l'image</div>
                                        <input type="file" id="event-image" name="images[]" class="upload-input"
                                            accept="image/*">
                                    </div>
                                    {% if errors.images ?? false %}
                                    <p class="form-error">{{ errors.images }}</p>
                                    {% endif %}
                                </div>
                            </div>

                            <div class="form-actions flex justify-end gap-4">
                                <a href="{{ path('president/events') }}" class="btn btn-outline">Annuler</a>
                                <button type="submit" class="btn btn-primary">Enregistrer</button>
                            </div>
                        </form>
                    </div>
                </div>
            </main>

            <footer class="app-footer">
                <p class="text-sm text-muted">2026 ClubEdge. Tous droits réservés.</p>
            </footer>
        </div>
    </div>

    <div class="mobile-overlay" id="mobile-overlay"></div>
    <script src="{{ asset('js/main.js') }}" defer></script>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const input = document.getElementById('event-image');
            const preview = document.getElementById('event-image-preview');
            const placeholder = document.getElementById('event-image-placeholder');
            if (!input || !preview || !placeholder) return;

            const setPreview = (file) => {
                if (!file) return;
                const reader = new FileReader();
                reader.onload = (e) => {
                    preview.src = e.target.result;
                    preview.classList.remove('hidden');
                    placeholder.classList.add('hidden');
                    input.closest('.upload-zone')?.classList.add('has-preview');
                };
                reader.readAsDataURL(file);
            };

            input.addEventListener('change', (e) => {
                const file = e.target.files && e.target.files[0];
                if (file) setPreview(file);
            });

            const zone = input.closest('.upload-zone');
            if (!zone) return;

            zone.addEventListener('dragover', (e) => {
                e.preventDefault();
                zone.classList.add('dragover');
            });
            zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
            zone.addEventListener('drop', (e) => {
                e.preventDefault();
                zone.classList.remove('dragover');
                const file = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
                if (file) {
                    const dt = new DataTransfer();
                    dt.items.add(file);
                    input.files = dt.files;
                    setPreview(file);
                }
            });
        });
    </script>

    <style>
        .upload-zone {
            position: relative;
            border: 2px dashed var(--border-color);
            border-radius: var(--radius-lg);
            background-color: var(--color-cool-slate);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            cursor: pointer;
            transition: all var(--duration-fast) var(--ease-default);
        }

        .upload-zone:hover {
            border-color: var(--color-accent);
            background-color: var(--color-accent-light);
        }

        .upload-zone.dragover {
            border-color: var(--color-accent);
            background-color: var(--color-accent-light);
        }

        .upload-zone.has-preview::before {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.35);
            opacity: 0;
            transition: opacity var(--duration-fast) var(--ease-default);
            z-index: 1;
        }

        .upload-zone.has-preview:hover::before,
        .upload-zone.has-preview:focus-within::before {
            opacity: 1;
        }

        .upload-icon {
            width: 40px;
            height: 40px;
            margin: 0 auto var(--space-3);
            color: var(--color-text-muted);
        }

        .upload-text {
            font-weight: var(--font-medium);
            color: var(--color-text-primary);
        }

        .upload-hint {
            margin-top: var(--space-1);
            font-size: var(--text-xs);
            color: var(--color-text-muted);
        }

        .upload-input {
            position: absolute;
            inset: 0;
            opacity: 0;
            cursor: pointer;
            z-index: 3;
        }

        .upload-preview {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .upload-placeholder {
            padding: var(--space-8);
            text-align: center;
        }

        .upload-overlay {
            position: absolute;
            inset: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--color-white);
            font-weight: var(--font-semibold);
            opacity: 0;
            z-index: 2;
            transition: opacity var(--duration-fast) var(--ease-default);
        }

        .upload-zone.has-preview:hover .upload-overlay,
        .upload-zone.has-preview:focus-within .upload-overlay {
            opacity: 1;
        }
    </style>
</body>

</html>
